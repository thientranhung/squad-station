---
phase: 13-safe-injection-and-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PLAYBOOK.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03

must_haves:
  truths:
    - "PLAYBOOK.md documents `squad-station signal $TMUX_PANE` as the canonical inline hook command (not a shell script path)"
    - "PLAYBOOK.md covers Antigravity provider with correct `tool: antigravity` squad.yml syntax"
    - "PLAYBOOK.md covers Notification hook registration with `permission_prompt` matcher for Claude Code"
    - "PLAYBOOK.md uses `tool:` field name in all squad.yml examples (not `provider:`)"
  artifacts:
    - path: "docs/PLAYBOOK.md"
      provides: "Complete v1.3 workflow documentation"
      contains: "squad-station signal $TMUX_PANE"
  key_links:
    - from: "PLAYBOOK.md hook setup section"
      to: "squad-station signal $TMUX_PANE"
      via: "inline command in settings.json Stop event"
      pattern: "signal \\$TMUX_PANE"
    - from: "PLAYBOOK.md Antigravity section"
      to: "tool: antigravity"
      via: "squad.yml syntax block"
      pattern: "tool: antigravity"
    - from: "PLAYBOOK.md Notification section"
      to: "permission_prompt"
      via: "Notification event matcher"
      pattern: "permission_prompt"
---

<objective>
Rewrite `docs/PLAYBOOK.md` as the authoritative v1.3 user guide, replacing stale shell-script hook references with the inline `squad-station signal $TMUX_PANE` command, adding the Antigravity provider mode, and covering Notification hook registration.

Purpose: The existing PLAYBOOK.md was written for v1.2 and references the deprecated `hooks/claude-code.sh` shell scripts. Users setting up v1.3 need the inline hook command, Antigravity IDE mode, and notification hook documentation.

Output: `docs/PLAYBOOK.md` fully rewritten. All three DOCS requirements covered.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-safe-injection-and-documentation/13-RESEARCH.md

<interfaces>
<!-- Authoritative sources for PLAYBOOK content. Use these directly. -->

Canonical inline hook form (DOCS-01) — from hooks/claude-code.sh deprecation notice:
  "squad-station signal $TMUX_PANE"

Live squad.yml field name (AUTHORITATIVE — use `tool:` not `provider:`):
  orchestrator.tool = "claude-code" | "antigravity"

Antigravity squad.yml example:
  orchestrator:
    tool: antigravity
    role: orchestrator
    description: "Orchestrator running inside Antigravity IDE."

Notification hook matcher for Claude Code (DOCS-03):
  "matcher": "permission_prompt"

CRITICAL PITFALLS:
- Do NOT use `"matcher": ""` (empty matcher) in inline completion hook examples
- Do NOT reference hooks/claude-code.sh as the setup method (deprecated since v1.3)
- Do NOT use `provider:` field in squad.yml examples — the field is `tool:`
- SOLUTION-DESIGN.md still has old `provider:` in code blocks — ignore those, follow live squad.yml
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite docs/PLAYBOOK.md as authoritative v1.3 guide</name>
  <files>docs/PLAYBOOK.md</files>
  <action>
Completely replace `docs/PLAYBOOK.md` with an authoritative v1.3 guide. The document must cover all sections below in order.

**Document structure (write each section completely):**

---

Section 1: Header + Prerequisites

Title: "# Squad Station Playbook"
Subtitle: "A step-by-step guide to orchestrating AI agent squads with squad-station."

Prerequisites block:
- tmux installed and available in PATH
- squad-station installed (`npm install -g squad-station` or via curl install script)
- At least one AI coding tool: Claude Code (`claude`) or Gemini CLI (`gemini`)

---

Section 2: "1. Define Your Squad"

Create a `squad.yml` in your project root. Show two examples:

**Standard CLI Orchestrator:**
```yaml
project: my-app

orchestrator:
  tool: claude-code
  role: orchestrator
  model: claude-opus-4-5
  description: "Lead orchestrator. Delegates tasks, synthesizes results."

agents:
  - name: frontend
    tool: claude-code
    role: worker
    model: claude-sonnet-4-5
    description: "Frontend specialist"
  - name: backend
    tool: claude-code
    role: worker
    model: claude-sonnet-4-5
    description: "Backend specialist"
```

**IDE Orchestrator (Antigravity):**
```yaml
project: my-app

orchestrator:
  tool: antigravity
  role: orchestrator
  description: >
    Orchestrator running inside Antigravity IDE.
    Uses Manager View to poll and monitor tmux worker agents.

agents:
  - name: implement
    tool: claude-code
    role: worker
    model: claude-sonnet-4-5
    description: "Implements features and fixes bugs"
```

Include the fields table (tool, role, model, description, name fields) matching the existing PLAYBOOK.

Include the agent naming convention note: `name` acts as a role suffix. Full registered name is `<project>-<tool>-<role_suffix>`.

---

Section 3: "2. Launch the Squad"

```bash
squad-station init
```

Explain: creates SQLite database, registers all agents, launches each agent in its own tmux session.

Show the --json output example (from existing PLAYBOOK §2).

Re-running init is safe -- already-running agents are skipped.

**Antigravity note:** When orchestrator tool is `antigravity`, init registers the orchestrator in the DB only (no tmux session is created for it) and prints a message confirming DB-only registration. Worker agents still get tmux sessions normally.

---

Section 4: "3. Set Up Completion Hooks"

Opening: "Hooks let squad-station know when an agent finishes its work. Without hooks, you must signal manually."

**Subsection: Automatic Setup**

`squad-station init` automatically sets up hooks:
- If a `settings.json` already exists, init merges the hook entry and creates a `.bak` backup before modifying
- If no `settings.json` exists, init prints the hook configuration to stdout for manual setup

**Subsection: Manual Setup — Claude Code**

Add to `.claude/settings.json` (project-level) or `~/.claude/settings.json` (global):

```json
{
  "hooks": {
    "Stop": [
      {
        "type": "command",
        "command": "squad-station signal $TMUX_PANE"
      }
    ]
  }
}
```

**Subsection: Manual Setup — Gemini CLI**

Add to `.gemini/settings.json`:

```json
{
  "hooks": {
    "AfterAgent": [
      {
        "type": "command",
        "command": "squad-station signal $TMUX_PANE"
      }
    ]
  }
}
```

Add notes:
- `signal` reads `$TMUX_PANE` to identify the agent automatically — no arguments needed beyond the env var
- The command always exits 0 and never blocks the AI tool, even on errors
- `hooks/claude-code.sh` and `hooks/gemini-cli.sh` are deprecated since v1.3 and kept for reference only. Use the inline command above.

---

Section 5: "4. Notification Hooks (Optional)"

Explain: When Claude Code encounters a permission prompt, it fires a `Notification` event. Hook this to alert yourself.

**Claude Code — permission prompt notifications:**

```json
{
  "hooks": {
    "Notification": [
      {
        "matcher": "permission_prompt",
        "hooks": [
          {
            "type": "command",
            "command": "hooks/claude-code-notify.sh"
          }
        ]
      }
    ]
  }
}
```

**Gemini CLI — notifications:**

```json
{
  "hooks": {
    "Notification": [
      {
        "type": "command",
        "command": "hooks/gemini-cli-notify.sh"
      }
    ]
  }
}
```

Note: Both notify scripts are included in the `hooks/` directory of the squad-station repo. Make them executable: `chmod +x hooks/claude-code-notify.sh hooks/gemini-cli-notify.sh`. Both scripts always exit 0.

---

Section 6: "5. Send Tasks to Agents"

Copy from existing PLAYBOOK §4 (Send Tasks to Agents). Preserve all examples, priority levels, and the what-happens list.

---

Section 7: "6. Monitor Your Squad"

Copy from existing PLAYBOOK §5 (Monitor Your Squad). Preserve all status/agents/list/ui/view examples.

---

Section 8: "7. Check Pending Work"

Copy from existing PLAYBOOK §6 (Check Pending Work). Preserve all peek examples.

---

Section 9: "8. Signal Completion"

Copy from existing PLAYBOOK §7 (Signal Completion). Keep note that hooks automate this. Keep manual signal example.

Update signal format note: "Duplicate signals are safe — they silently succeed."

---

Section 10: "9. Antigravity IDE Orchestrator Mode"

**When to use:**
Use `tool: antigravity` when you want to run the orchestrator inside an IDE (Antigravity, Cursor, VS Code agent, etc.) rather than as a CLI tmux session.

**What changes with Antigravity:**
- `init` registers the orchestrator in the DB only — no tmux session is created for it
- `signal` updates the message status in the DB but does NOT inject a notification into a tmux session (there is none)
- The IDE polls completion by calling `squad-station status` or `squad-station list --status completed`
- Worker agents still run as tmux sessions and receive tasks via the normal send path

**squad.yml (full example):**

```yaml
project: my-app

orchestrator:
  tool: antigravity
  role: orchestrator
  description: >
    Orchestrator running inside Antigravity IDE.
    Uses Manager View to poll and monitor tmux worker agents.

agents:
  - name: implement
    tool: claude-code
    role: worker
    model: claude-sonnet-4-5
    description: "Implements features and fixes bugs"
```

**IDE workflow:**

1. Run `squad-station init` — registers orchestrator in DB, launches worker tmux sessions
2. Run `squad-station context` — generates `.agent/workflows/` files for the IDE orchestrator
3. IDE orchestrator reads `.agent/workflows/squad-delegate.md` and `.agent/workflows/squad-roster.md`
4. IDE orchestrator calls `squad-station send <agent> --body "..."` to dispatch tasks
5. IDE orchestrator polls `squad-station status` or `squad-station list --status completed` to detect task completion

**Context files generated by `squad-station context`:**
- `.agent/workflows/squad-delegate.md` — delegation instructions and exact CLI commands
- `.agent/workflows/squad-monitor.md` — polling/monitoring guidance with behavioral rules
- `.agent/workflows/squad-roster.md` — agent roster with names, models, descriptions

---

Section 11: "10. Register Agents at Runtime"

Copy from existing PLAYBOOK §8 (Register Agents at Runtime). No changes needed.

---

Section 12: "11. Generate Orchestrator Context"

Based on existing PLAYBOOK §9, update to mention the .agent/workflows/ files:

```bash
squad-station context
```

For CLI orchestrators: outputs a Markdown document to stdout with the agent roster and usage examples. Feed this to your orchestrator.

For IDE orchestrators (Antigravity): also writes three files to `.agent/workflows/`:
- `squad-delegate.md` — delegation instructions and exact CLI commands
- `squad-monitor.md` — monitoring/polling guidance
- `squad-roster.md` — agent roster listing names, models, and descriptions

Show the example context output from the existing PLAYBOOK.

---

Section 13: Workflow Summary

Copy the ASCII workflow diagram from the existing PLAYBOOK unchanged.

---

Section 14: Command Reference

Copy the command reference table from the existing PLAYBOOK unchanged.

---

Section 15: Troubleshooting

Copy all existing troubleshooting entries. Add two new entries at the end:

**Hook not firing (inline command)**
Verify `$TMUX_PANE` is set in the agent session: run `echo $TMUX_PANE` inside the agent tmux session. Also verify `squad-station` is in PATH: `which squad-station`. The inline hook requires no script path — only the binary being accessible.

**Antigravity: orchestrator not receiving completion signals**
This is expected behavior. With `tool: antigravity`, the orchestrator has no tmux session, so signal does not inject a tmux notification. Use `squad-station status` or `squad-station list --status completed` to poll for task completion instead.
  </action>
  <verify>
    <automated>test -f /Users/tranthien/Documents/2.DEV/2.PRIVATE/squad-station/docs/PLAYBOOK.md &amp;&amp; grep -q "signal" /Users/tranthien/Documents/2.DEV/2.PRIVATE/squad-station/docs/PLAYBOOK.md &amp;&amp; grep -q "antigravity" /Users/tranthien/Documents/2.DEV/2.PRIVATE/squad-station/docs/PLAYBOOK.md &amp;&amp; grep -q "permission_prompt" /Users/tranthien/Documents/2.DEV/2.PRIVATE/squad-station/docs/PLAYBOOK.md &amp;&amp; grep -q "Notification" /Users/tranthien/Documents/2.DEV/2.PRIVATE/squad-station/docs/PLAYBOOK.md &amp;&amp; echo "DOCS-01/02/03 pass"</automated>
  </verify>
  <done>
    `docs/PLAYBOOK.md` exists and contains: "squad-station signal $TMUX_PANE" (DOCS-01), "antigravity" and "tool: antigravity" in squad.yml syntax block (DOCS-02), "Notification" and "permission_prompt" in notification hook section (DOCS-03). No reference to hooks/claude-code.sh as the setup method. All squad.yml examples use `tool:` not `provider:`.
  </done>
</task>

</tasks>

<verification>
Final check after task completes:

```
test -f docs/PLAYBOOK.md
grep -c "signal" docs/PLAYBOOK.md        # >= 1
grep -c "antigravity" docs/PLAYBOOK.md   # >= 2 (squad.yml examples + prose)
grep -c "permission_prompt" docs/PLAYBOOK.md  # >= 1
grep -c "Notification" docs/PLAYBOOK.md  # >= 1
grep -c "tool: antigravity" docs/PLAYBOOK.md  # >= 1 (correct field name)
grep "provider:" docs/PLAYBOOK.md | grep -v "#" || echo "No provider: field in examples — correct"
```
</verification>

<success_criteria>
- `docs/PLAYBOOK.md` exists
- Contains inline hook command `squad-station signal $TMUX_PANE` for both Claude Code (Stop event) and Gemini CLI (AfterAgent event)
- Contains Antigravity section with `tool: antigravity` squad.yml syntax
- Contains Notification hook section with `permission_prompt` matcher for Claude Code
- All squad.yml examples use `tool:` not `provider:`
- No reference to shell script paths as the canonical hook setup method
- Troubleshooting covers both inline hook issues and Antigravity polling behavior
</success_criteria>

<output>
After completion, create `.planning/phases/13-safe-injection-and-documentation/13-02-SUMMARY.md`
</output>
