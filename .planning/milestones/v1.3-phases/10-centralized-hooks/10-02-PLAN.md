---
phase: 10-centralized-hooks
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/claude-code.sh
  - hooks/gemini-cli.sh
autonomous: true
requirements:
  - HOOK-02

must_haves:
  truths:
    - "hooks/claude-code.sh file header contains a DEPRECATED notice with the inline command alternative"
    - "hooks/gemini-cli.sh file header contains a DEPRECATED notice with the inline command alternative"
    - "Both scripts remain functional — no executable logic is changed"
  artifacts:
    - path: "hooks/claude-code.sh"
      provides: "Deprecation header above existing script body"
      contains: "DEPRECATED"
    - path: "hooks/gemini-cli.sh"
      provides: "Deprecation header above existing script body"
      contains: "DEPRECATED"
  key_links:
    - from: "hooks/claude-code.sh line 1 shebang"
      to: "DEPRECATED notice block"
      via: "Comment lines 2-9 before existing header comment"
      pattern: "DEPRECATED"
---

<objective>
Implement HOOK-02: add deprecation notices to the two provider hook shell scripts. The scripts are kept intact for backward compatibility and reference, but their file headers now clearly state they are deprecated as of v1.3 and show the inline command alternative.

Purpose: Users upgrading from older setups see the deprecation notice immediately when they open the scripts, and know exactly what to use instead.

Output: hooks/claude-code.sh and hooks/gemini-cli.sh with deprecation headers prepended. Zero functional changes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-centralized-hooks/10-RESEARCH.md
</context>

<interfaces>
<!-- Current file headers — deprecation block is prepended AFTER the shebang line -->

hooks/claude-code.sh (CURRENT lines 1-8):
```bash
#!/bin/bash
# hooks/claude-code.sh -- Signal squad-station when Claude Code finishes a response
# Registered under Stop event in .claude/settings.json or ~/.claude/settings.json
#
# Claude Code passes JSON via stdin (we discard it -- we only need the tmux session name)
# All guard logic (not-in-tmux, unregistered agent, orchestrator skip) is in the Rust binary.
# This script ALWAYS exits 0 -- exit 2 would prevent Claude from stopping (catastrophic).
```

hooks/gemini-cli.sh (CURRENT lines 1-8):
```bash
#!/bin/bash
# hooks/gemini-cli.sh -- Signal squad-station when Gemini CLI finishes a response
# Registered under AfterAgent event in .gemini/settings.json
#
# Gemini CLI passes JSON via stdin (we discard it -- we only need the tmux session name)
# All guard logic is in the Rust binary. This script ALWAYS exits 0.
# Exit 2 would trigger Gemini CLI automatic retry -- equally catastrophic.
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add deprecation headers to both hook scripts</name>
  <files>hooks/claude-code.sh, hooks/gemini-cli.sh</files>
  <action>
For each file, insert the deprecation block AFTER the `#!/bin/bash` shebang line (line 1) and BEFORE the existing description header comment. The deprecation block is:

```bash
# DEPRECATED: This script is no longer required as of squad-station v1.3.
# Use the inline hook command instead (register directly in settings.json):
#
#   squad-station signal $TMUX_PANE
#
# This file is kept for reference only. It remains functional for users on older setups.
```

Final result for hooks/claude-code.sh (first 14 lines):
```bash
#!/bin/bash
# DEPRECATED: This script is no longer required as of squad-station v1.3.
# Use the inline hook command instead (register directly in settings.json):
#
#   squad-station signal $TMUX_PANE
#
# This file is kept for reference only. It remains functional for users on older setups.
#
# hooks/claude-code.sh -- Signal squad-station when Claude Code finishes a response
# Registered under Stop event in .claude/settings.json or ~/.claude/settings.json
#
# Claude Code passes JSON via stdin (we discard it -- we only need the tmux session name)
# All guard logic (not-in-tmux, unregistered agent, orchestrator skip) is in the Rust binary.
# This script ALWAYS exits 0 -- exit 2 would prevent Claude from stopping (catastrophic).
```

Final result for hooks/gemini-cli.sh (first 14 lines):
```bash
#!/bin/bash
# DEPRECATED: This script is no longer required as of squad-station v1.3.
# Use the inline hook command instead (register directly in settings.json):
#
#   squad-station signal $TMUX_PANE
#
# This file is kept for reference only. It remains functional for users on older setups.
#
# hooks/gemini-cli.sh -- Signal squad-station when Gemini CLI finishes a response
# Registered under AfterAgent event in .gemini/settings.json
#
# Gemini CLI passes JSON via stdin (we discard it -- we only need the tmux session name)
# All guard logic is in the Rust binary. This script ALWAYS exits 0.
# Exit 2 would trigger Gemini CLI automatic retry -- equally catastrophic.
```

All executable logic (everything after the header block: stdin drain, TMUX_PANE check, session name detection, binary invocation) remains unchanged.
  </action>
  <verify>
    <automated>grep -q "DEPRECATED" /Users/tranthien/Documents/2.DEV/2.PRIVATE/squad-station/hooks/claude-code.sh && grep -q "DEPRECATED" /Users/tranthien/Documents/2.DEV/2.PRIVATE/squad-station/hooks/gemini-cli.sh && echo "HOOK-02: both scripts have deprecation notice"</automated>
  </verify>
  <done>
    - `grep -q DEPRECATED hooks/claude-code.sh` exits 0
    - `grep -q DEPRECATED hooks/gemini-cli.sh` exits 0
    - Both scripts contain `squad-station signal $TMUX_PANE` in the deprecation block
    - Both scripts remain syntactically valid bash (`bash -n hooks/claude-code.sh` and `bash -n hooks/gemini-cli.sh` exit 0)
    - All executable logic below the header is identical to the original
  </done>
</task>

</tasks>

<verification>
```bash
bash -n /Users/tranthien/Documents/2.DEV/2.PRIVATE/squad-station/hooks/claude-code.sh && echo "syntax OK"
bash -n /Users/tranthien/Documents/2.DEV/2.PRIVATE/squad-station/hooks/gemini-cli.sh && echo "syntax OK"
grep "squad-station signal" /Users/tranthien/Documents/2.DEV/2.PRIVATE/squad-station/hooks/claude-code.sh
grep "squad-station signal" /Users/tranthien/Documents/2.DEV/2.PRIVATE/squad-station/hooks/gemini-cli.sh
```
Both scripts must pass `bash -n` syntax check and contain the inline command example.
</verification>

<success_criteria>
1. Both hooks/claude-code.sh and hooks/gemini-cli.sh have "DEPRECATED" in file header
2. Both scripts show `squad-station signal $TMUX_PANE` as the replacement command
3. `bash -n` syntax check passes for both files (no executable logic was altered)
</success_criteria>

<output>
After completion, create `.planning/phases/10-centralized-hooks/10-02-SUMMARY.md`
</output>
