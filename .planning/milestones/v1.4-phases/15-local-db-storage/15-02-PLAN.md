---
phase: 15-local-db-storage
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - .gitignore
  - CLAUDE.md
  - README.md
autonomous: true
requirements:
  - LODB-03
  - LODB-06

must_haves:
  truths:
    - ".gitignore contains .squad/ entry"
    - "CLAUDE.md documents .squad/station.db as the DB location"
    - "README.md documents .squad/station.db as the DB location"
    - "No references to ~/.agentic-squad/ remain in CLAUDE.md or README.md"
  artifacts:
    - path: ".gitignore"
      provides: ".squad/ exclusion entry"
      contains: ".squad/"
    - path: "CLAUDE.md"
      provides: "Updated DB path documentation"
      contains: ".squad/station.db"
    - path: "README.md"
      provides: "Updated DB path documentation"
      contains: ".squad/station.db"
  key_links: []
---

<objective>
Update .gitignore and documentation to reflect the new `.squad/station.db` local DB path.

Purpose: Prevent accidental DB commits and ensure docs match the new reality.
Output: Updated .gitignore, CLAUDE.md, README.md.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.gitignore
@CLAUDE.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .squad/ to .gitignore and update docs</name>
  <files>.gitignore, CLAUDE.md, README.md</files>
  <action>
1. In `.gitignore`, add a new section at the end:
   ```
   # Squad Station local DB
   .squad/
   ```

2. In `CLAUDE.md`, find the sentence:
   `Each project gets its own DB at ~/.agentic-squad/<project-name>/station.db`
   Replace with:
   `Each project gets its own DB at .squad/station.db inside the project directory`

3. In `README.md`, find the sentence:
   `Each project stores its state in its own SQLite database at ~/.agentic-squad/<project>/station.db`
   Replace with:
   `Each project stores its state in a local SQLite database at .squad/station.db inside the project directory`

4. Verify no other references to `~/.agentic-squad/` remain in CLAUDE.md or README.md by running grep.
  </action>
  <verify>
    <automated>cd /Users/tranthien/Documents/2.DEV/2.PRIVATE/squad-station && grep -c ".squad/" .gitignore && grep -c ".squad/station.db" CLAUDE.md README.md && grep -c "agentic-squad" CLAUDE.md README.md | grep -v ":0$" ; echo "exit: $?"</automated>
  </verify>
  <done>
    - .gitignore has `.squad/` entry
    - CLAUDE.md says `.squad/station.db` (no `~/.agentic-squad/` references)
    - README.md says `.squad/station.db` (no `~/.agentic-squad/` references)
  </done>
</task>

</tasks>

<verification>
- `grep ".squad/" .gitignore` returns the entry
- `grep "agentic-squad" CLAUDE.md README.md` returns nothing
- `grep ".squad/station.db" CLAUDE.md README.md` returns matches in both files
</verification>

<success_criteria>
- .squad/ is gitignored
- All user-facing docs reference the new .squad/station.db path
- No stale references to ~/.agentic-squad/ in CLAUDE.md or README.md
</success_criteria>

<output>
After completion, create `.planning/phases/15-local-db-storage/15-02-SUMMARY.md`
</output>
