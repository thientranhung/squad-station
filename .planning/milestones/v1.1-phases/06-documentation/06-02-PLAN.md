---
phase: 06-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PLAYBOOK.md
autonomous: true
requirements:
  - DOCS-02

must_haves:
  truths:
    - "PLAYBOOK.md shows send --body flag syntax, not positional task argument"
    - "PLAYBOOK.md squad.yml examples use tool field, not provider"
    - "PLAYBOOK.md squad.yml examples have no command field"
    - "PLAYBOOK.md shows project as a flat string, not nested struct"
    - "PLAYBOOK.md documents <project>-<tool>-<role_suffix> agent naming convention"
    - "PLAYBOOK.md context section shows Markdown section-per-agent format, not table"
    - "PLAYBOOK.md register command uses --tool flag, not --provider"
    - "PLAYBOOK.md signal format shows <agent> completed <msg-id>"
  artifacts:
    - path: "docs/PLAYBOOK.md"
      provides: "Correct user-facing CLI reference for post-v1.1 squad-station"
      contains: "--body"
  key_links:
    - from: "docs/PLAYBOOK.md"
      to: "src/cli.rs"
      via: "all command syntax examples match actual clap definitions"
      pattern: "send.*--body"
    - from: "docs/PLAYBOOK.md"
      to: "src/config.rs"
      via: "squad.yml examples match actual SquadConfig/AgentConfig structs"
      pattern: "tool:"
---

<objective>
Rewrite `docs/PLAYBOOK.md` to reflect the post-v1.1 CLI syntax, config format, and agent naming convention.

Purpose: PLAYBOOK.md was written for v1.0 and contains stale content across multiple sections: positional send argument, provider field, command field, nested project struct, old register flag, old context table output, and old signal format. A user following the playbook will encounter errors because the CLI no longer matches what is documented.

Output: A corrected PLAYBOOK.md where every command example, squad.yml snippet, field table, and workflow diagram matches the actual v1.1 implementation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claire/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-documentation/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PLAYBOOK.md with post-v1.1 CLI syntax and config format</name>
  <files>docs/PLAYBOOK.md</files>
  <action>
Update `docs/PLAYBOOK.md` by correcting every stale section. The 06-RESEARCH.md file (in context) provides the complete gap inventory under "DOCS-02: PLAYBOOK.md vs Actual Code" and the exact replacement content in "Code Examples". Do not delete sections — update them in place.

Apply every correction from the RESEARCH.md gap table. Each row is a specific find-and-replace with context:

**Section 1 — squad.yml example block:**
Replace the nested project struct and stale fields with:
```yaml
project: my-app

orchestrator:
  tool: claude-code
  role: orchestrator
  model: claude-opus-4-5           # optional
  description: "Lead orchestrator" # optional

agents:
  - name: frontend                  # role suffix; full name = my-app-claude-code-frontend
    tool: claude-code
    role: worker
    model: claude-sonnet-4-5        # optional
    description: "Frontend specialist"
  - name: backend
    tool: gemini
    role: worker
```
Note: `project: my-app` is a plain string (CONF-01). Field is `tool` not `provider` (CONF-04). No `command` field (CONF-03).

**Section 1 — fields table:**
- Replace `*.provider` → `*.tool` (required)
- Remove `*.command` row entirely
- Remove `project.db_path` row (DB path is controlled by SQUAD_STATION_DB env var only, not in squad.yml)
- Update `*.name` description: "Acts as role suffix; full agent name is auto-prefixed as `<project>-<tool>-<role_suffix>` (e.g., `my-app-claude-code-frontend`)"

**Section 4 (send command):**
Replace every occurrence of positional task argument with `--body` flag:
- `squad-station send frontend-dev "Build the login page"` → `squad-station send frontend-dev --body "Build the login page"`
- Apply to ALL three send examples in this section
- Add note: task body is a required named flag (`--body`), not a positional argument

**Section 8 (register command):**
- `--provider claude-code` → `--tool claude-code`
- Update any register examples that show `--provider` flag

**Section 9 (context command):**
Replace the table output format with Markdown section format:
```
# Squad Station -- Agent Roster

## Available Agents

## my-app-claude-code-frontend (claude-sonnet-4-5)

Frontend specialist

Role: worker | Status: idle

→ squad-station send my-app-claude-code-frontend --body "..."

---

## Usage

Send a task to an agent:
```
squad-station send <agent> --body "<task description>"
```
```
Explain: context output is one Markdown section per agent (CLI-03), not a table.

**Workflow diagram (if present):**
- `squad-station send agent "task"` → `squad-station send agent --body "task"`
- `[SIGNAL] agent=X status=completed task_id=Y` → `<agent> completed <msg-id>` (SIG-01)

**Command Reference table (if present):**
- `send <agent> <task>` → `send <agent> --body <task>`
- `register <name>` with `--provider` flag → `register <name>` with `--tool` flag

**Agent naming section (add if not present, update if present):**
Add a note explaining the naming convention: the `name` field in squad.yml acts as a role suffix. The full registered name is `<project>-<tool>-<role_suffix>`. Example: project `my-app`, tool `claude-code`, name `frontend` → registered as `my-app-claude-code-frontend`.

After all changes, grep the file for `provider` — there must be zero remaining occurrences (the word should not appear anywhere in the updated file).
  </action>
  <verify>
    <automated>cargo check 2>&amp;1 | tail -3 &amp;&amp; grep -c "send.*--body" docs/PLAYBOOK.md &amp;&amp; grep -c "provider" docs/PLAYBOOK.md || true</automated>
  </verify>
  <done>
- `grep "send.*--body" docs/PLAYBOOK.md` returns at least 1 match
- `grep -c "provider" docs/PLAYBOOK.md` returns 0
- `grep -c "command:" docs/PLAYBOOK.md` in squad.yml examples returns 0 (no command field shown)
- `grep "tool:" docs/PLAYBOOK.md` returns at least 1 match
- `grep "project: my-app" docs/PLAYBOOK.md` matches (flat string format)
- `grep "\-\-tool" docs/PLAYBOOK.md` matches (register flag updated)
- `grep "completed" docs/PLAYBOOK.md` shows new signal format pattern
- `cargo check` exits 0 (no Rust code was touched)
  </done>
</task>

</tasks>

<verification>
1. `cargo check` passes — no Rust source accidentally changed
2. `grep -c "provider" docs/PLAYBOOK.md` returns 0 (zero stale provider references)
3. `grep "send.*--body" docs/PLAYBOOK.md` returns matches (--body flag documented)
4. `grep "tool:" docs/PLAYBOOK.md` returns matches (tool field present)
5. `grep "project: my-app" docs/PLAYBOOK.md` matches (flat project string)
6. Manual: Section 9 shows Markdown sections per agent, not a table
7. Manual: No squad.yml examples contain a `command:` field
8. Manual: Signal format section shows `<agent> completed <msg-id>` not [SIGNAL] format
</verification>

<success_criteria>
PLAYBOOK.md accurately describes the v1.1 CLI:
- All send examples use `--body` flag (not positional argument)
- All squad.yml examples use `tool` field (not `provider`), no `command` field, `project` as plain string
- Register command shows `--tool` flag
- Agent naming convention (`<project>-<tool>-<role_suffix>`) is explained
- Context output section shows Markdown section-per-agent format
- Signal format shows `<agent> completed <msg-id>`
- Zero occurrences of the word "provider" in the file
</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation/06-02-SUMMARY.md` following the summary template.
</output>
