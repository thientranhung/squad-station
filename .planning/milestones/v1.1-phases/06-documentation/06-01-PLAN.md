---
phase: 06-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/ARCHITECTURE.md
autonomous: true
requirements:
  - DOCS-01

must_haves:
  truths:
    - "ARCHITECTURE.md describes sqlx async pool, not rusqlite"
    - "ARCHITECTURE.md shows flat module layout matching the actual src/ directory tree"
    - "ARCHITECTURE.md contains no references to src/tui/, src/orchestrator/, or src/tmux/ subdirectories"
    - "ARCHITECTURE.md shows the correct DB schema columns (tool, model, description, current_task, from_agent, to_agent)"
    - "ARCHITECTURE.md documents the three migration files (0001, 0002, 0003)"
  artifacts:
    - path: ".planning/research/ARCHITECTURE.md"
      provides: "Accurate architecture reference for the post-v1.1 codebase"
      contains: "sqlx"
  key_links:
    - from: ".planning/research/ARCHITECTURE.md"
      to: "src/db/mod.rs"
      via: "sqlx::SqlitePool description matches actual connect() implementation"
      pattern: "sqlx.*SqlitePool"
    - from: ".planning/research/ARCHITECTURE.md"
      to: "src/ directory"
      via: "module layout section matches actual flat file structure"
      pattern: "src/tmux\\.rs"
---

<objective>
Rewrite `.planning/research/ARCHITECTURE.md` to accurately describe the post-v1.1 codebase instead of the original planned design.

Purpose: The file was written during initial design research (2026-03-06) and describes a planned architecture that was never implemented: rusqlite + subdirectory modules (src/db/agent_repo.rs, src/tui/, src/orchestrator/). The actual implementation uses sqlx async pool with flat module files and a different DB schema. This misleads anyone using ARCHITECTURE.md to understand the codebase.

Output: A replaced ARCHITECTURE.md that matches src/ exactly — correct library (sqlx), correct flat file layout, correct DB schema (post-migration 0003), correct signal flow.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-documentation/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite ARCHITECTURE.md with accurate post-v1.1 content</name>
  <files>.planning/research/ARCHITECTURE.md</files>
  <action>
Completely replace the content of `.planning/research/ARCHITECTURE.md`. The existing file describes a planned design that was never implemented. Write a new document reflecting the actual codebase.

The 06-RESEARCH.md file (already in context) contains everything needed:
- "Actual Module Structure" section — use this exact directory tree
- "Actual DB Schema (post-migration 0003)" — use the full SQL for both tables
- "sqlx Pool Pattern" — use the connect() code block
- The gap analysis table documents every stale claim; each stale claim should simply not appear in the new document

Structure the new ARCHITECTURE.md with these sections:

## Overview
Brief: stateless Rust CLI, sqlx async SQLite (WAL mode), flat module files. Core purpose: route messages between orchestrator and N agents via tmux. No daemon.

## Module Layout
Use the exact directory tree from RESEARCH.md "Actual Module Structure" section. Include one-line descriptions for each file exactly as shown there.

## Key Dependencies
- sqlx 0.7 (async SQLite pool, compile-time query checking, sqlx::migrate!())
- clap (argument parsing, Commands enum)
- ratatui (TUI dashboard in ui.rs)
- anyhow (error propagation)
- tokio (async runtime, single-threaded)
- uuid (message IDs)
- serde / serde_yaml (squad.yml deserialization)
- Direct tmux CLI calls via std::process::Command (no tmux crate)

## Database Layer
Include the full connect() code block from RESEARCH.md. Include both SQL CREATE TABLE statements (agents and messages) from RESEARCH.md "Actual DB Schema" section. Note that migration 0003_v11.sql adds the v1.1 columns via ALTER TABLE.

List the three migration files with their purpose:
- 0001_initial.sql — agents + messages base tables
- 0002_agent_status.sql — status_updated_at column
- 0003_v11.sql — v1.1 schema: tool rename, model/description/current_task, from_agent/to_agent/type/completed_at

## Config Format
Show the correct squad.yml format from RESEARCH.md "Code Examples" section. Note: project is a plain string (not nested), field is `tool` (not `provider`), no `command` field.

## Agent Naming
Document the <project>-<tool>-<role_suffix> convention enforced by init.rs. The `name` field in squad.yml acts as the role suffix.

## Key Command Flows

**send flow:** CLI receives `--body` flag (named, not positional) → insert_message() → update agent status to busy → tmux send-keys-literal to agent session.

**signal flow:** signal command receives agent + msg-id → update message to completed + agent to idle → inject plain string `"<agent> completed <msg-id>"` into orchestrator tmux session (NOT the old [SIGNAL] format).

**init flow:** Load squad.yml → derive full agent names as `<project>-<tool>-<role_suffix>` → INSERT OR IGNORE into agents → launch tmux sessions.

Do NOT include any of the following (all stale):
- rusqlite, rusqlite_migration references
- src/tui/ subdirectory
- src/orchestrator/ subdirectory
- src/tmux/ subdirectory (tmux is a flat src/tmux.rs file)
- src/db/agent_repo.rs or message_repo.rs or models.rs or migrations.rs module
- message_log table name (actual name is messages)
- session, registered_at, last_signal_at columns
- tmux_interface crate
- provider field (renamed to tool)
- [SIGNAL] agent=X status=completed task_id=Y format
- rusqlite::Connection synchronous pattern
- orchestrator::generate_context() module reference
  </action>
  <verify>
    <automated>cargo check 2>&amp;1 | tail -3 &amp;&amp; grep -c "sqlx" .planning/research/ARCHITECTURE.md &amp;&amp; grep -c "rusqlite" .planning/research/ARCHITECTURE.md || true</automated>
  </verify>
  <done>
- `.planning/research/ARCHITECTURE.md` contains "sqlx" (grep count > 0)
- `.planning/research/ARCHITECTURE.md` contains zero "rusqlite" occurrences
- File shows flat src/ layout: src/tmux.rs (not src/tmux/ subdir)
- File shows correct DB column names (tool, model, description, current_task)
- File shows correct migration files (0001, 0002, 0003)
- `cargo check` exits 0 (no code was accidentally changed)
  </done>
</task>

</tasks>

<verification>
1. `cargo check` passes — confirms no Rust source was accidentally modified
2. `grep -c "rusqlite" .planning/research/ARCHITECTURE.md` returns 0
3. `grep -c "sqlx" .planning/research/ARCHITECTURE.md` returns > 0
4. `grep "src/tmux.rs" .planning/research/ARCHITECTURE.md` matches (flat file, not subdir)
5. `grep "tool" .planning/research/ARCHITECTURE.md` matches (not "provider")
6. Manual review: no src/tui/, src/orchestrator/, or src/tmux/ subdirectory references remain
</verification>

<success_criteria>
ARCHITECTURE.md accurately describes the actual post-v1.1 codebase:
- sqlx async pool with WAL mode (not rusqlite)
- Flat module files matching actual src/ directory
- Correct DB schema (tool, model, description, current_task, from_agent, to_agent, type, completed_at)
- Correct signal format: `<agent> completed <msg-id>`
- Correct agent naming: `<project>-<tool>-<role_suffix>`
- Zero stale references from the planned-but-not-built design
</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation/06-01-SUMMARY.md` following the summary template.
</output>
